/* easy.js */

function addToStart(num, arr) {
  // return the array with a number added to the start

}

function makeArrayCopy(arr) {
  // return a new array which is a copy 
  // of the old array

}

function getLastElement(arr) {
  // remove the last element of an 
  // array and return a string that says
  // "Array element was in position X and was value Y"
  // where X is the index of that element and
  // Y is the value

}

function getEvens(arr) {
  // return a new array with
  // only even values in it
  // assume input array is all numeric
  // values

}

function rotateClockwise(arr) {
  // return a new array that is a copy
  // of the old array, with the elements
  // rotated so the last element is now
  // the first element

}



function checkIfHigher(num, arr) {
  // return true if there exists at least 
  // one number in the array that is 
  // larger than or equal to num
  // or return false

}

function checkFactors(num, arr) {
  // return true if all integers in 
  // an array are factors of a number 
  // or return false if one or more
  // are not factors

}

function findIndex(str, arr) {
  // in an array of strings,
  // return the index of the
  // first occurence of a given string
  // or return null if the string is
  // not in the array

}

function countDown(num) {
  // take a given positive number and
  // return an array of numbers counting 
  // down from num to zero

}

module.exports = {
  addToStart,
  makeArrayCopy,
  getLastElement,
  getEvens,
  rotateClockwise,
  checkIfHigher,
  checkFactors,
  findIndex,
  countDown,
};