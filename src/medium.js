/* medium.js */

function makeMirrorArray(arr) {
  // given an array, return a new array
  // that is a mirror
  // so [ 1, 2, 3] returns [1, 2, 3, 2, 1]

}

function makeStrToArray(str, delim) {
  // given a string and a delimiter
  // break up the string into an array
  // of elements
  // return the new array

}

function getAbsSum(nums) {
  // return the sum of the absolute value of each element
  // of a given array called nums

}

function removeStrings(arr) {
  // return a new array 
  // without the strings from an
  // array that might have strings in it

}

function findMinMax(arr) {
  // return a 2-element array
  // where the first number is the minimum
  // and the second number is the maximum
  // of a given array that has two elements
  // or more

}

function getAverage(arr) {
  // return a numerical average
  // to 2 decimal places where
  // it is the average
  // of a list of numbers inside
  // a given array

}

function getTelNo(numbers) {
  //return a string from an array of 10 digit elements
  // where the string is formatted as a phone number
  // like this: (202)456-1111

}

module.exports = {
  makeMirrorArray,
  makeStrToArray,
  getAbsSum,
  removeStrings,
  findMinMax,
  getAverage,
  getTelNo,
};